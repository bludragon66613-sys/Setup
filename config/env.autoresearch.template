# Autoresearch automation env vars
# Telegram chat ID for nightly crash/completion alerts via OpenClaw
# Get your chat ID by messaging @kaneda6bot then running: curl 'http://localhost:18789/telegram/updates' | jq '.result[-1].message.chat.id'
TELEGRAM_CHAT_ID=

# Override default project scan paths (space-separated)
# PROJECT_DIRS="$HOME/aeon $HOME/paperclip $HOME/nts"
